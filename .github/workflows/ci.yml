name: CI con Self-Hosted Runner

on:
  push:
    branches:
      - deploy/prueba1

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v2
      - name: Run a script
        run: echo "Hello from the self-hosted runner!"
